import { Controller, Get } from '@nestjs/common';

@Controller('commits')
export class CommitsController {
  @Get()
  async getAllCommits(

  ): Promise<any> {
    try {
      
    } catch (error) {
      
    }
  }
}
